/**
 * Generates a route with a proper uri.
 *
 * @export
 * @param {string} base - The base route.
 * @param {string} route - The extending route.
 * @returns {string}
 */
export declare function GenerateURI(base: string, route: string): string;
